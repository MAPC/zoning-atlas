import React from 'react';
import activeFilter from '../../images/activeFilter.svg';

const ActiveFilterIcon = () => (
  <img src={activeFilter} alt="Decorative indication of active filter" className="filters__active-icon" />
);

export default ActiveFilterIcon;
